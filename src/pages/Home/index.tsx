import * as React from 'react';
import Layout from '../../components/Layout';

const Home = (): JSX.Element => {
    return (
        <Layout>
            <p>ようこそ</p>
        </Layout>
    );
};

export default Home;